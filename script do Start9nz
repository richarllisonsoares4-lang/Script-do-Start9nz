-- =============================================
-- SCRIPT ST V4.7 - BOTÕES VISÍVEIS + HUB FLUTUANTE
-- =============================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local StarterGui = game:GetService("StarterGui")
local HttpService = game:GetService("HttpService")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ST_Hub"
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.Parent = PlayerGui

-- ==================== MAIN FRAME ====================
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 520, 0, 620)
MainFrame.Position = UDim2.new(0.5, -260, 0.5, -310)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 40)
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui

-- Draggable
local dragging = false
local dragStart, startPos
MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
    end
end)
MainFrame.InputChanged:Connect(function(input)
    if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end
end)

-- Rainbow Title
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1,0,0,60)
Title.BackgroundColor3 = Color3.fromRGB(0, 80, 200)
Title.Text = "SCRIPT DO START BB - ST"
Title.TextColor3 = Color3.new(1,1,1)
Title.TextScaled = true
Title.Font = Enum.Font.GothamBold
Title.Parent = MainFrame

local rainbowTick = 0
RunService.RenderStepped:Connect(function(dt)
    rainbowTick += dt * 1.5
    Title.BackgroundColor3 = Color3.fromHSV((rainbowTick % 6)/6, 1, 1)
end)

-- Tabs Bar
local TabFrame = Instance.new("Frame")
TabFrame.Size = UDim2.new(1,0,0,50)
TabFrame.Position = UDim2.new(0,0,0,60)
TabFrame.BackgroundColor3 = Color3.fromRGB(15,15,35)
TabFrame.Parent = MainFrame

local TabContainer = Instance.new("Frame")
TabContainer.Size = UDim2.new(1,0,1,0)
TabContainer.Position = UDim2.new(0,0,0,110)
TabContainer.BackgroundTransparency = 1
TabContainer.Parent = MainFrame

local function CreateTab(name)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0, 170, 1, 0)
    btn.BackgroundColor3 = Color3.fromRGB(30,30,60)
    btn.Text = name
    btn.TextColor3 = Color3.new(1,1,1)
    btn.TextScaled = true
    btn.Font = Enum.Font.GothamSemibold
    btn.Parent = TabFrame

    local page = Instance.new("ScrollingFrame")
    page.Size = UDim2.new(1,0,1,0)
    page.BackgroundTransparency = 1
    page.ScrollBarThickness = 6
    page.Visible = false
    page.Parent = TabContainer

    local layout = Instance.new("UIListLayout")
    layout.Padding = UDim.new(0, 8)
    layout.SortOrder = Enum.SortOrder.LayoutOrder
    layout.Parent = page

    btn.MouseButton1Click:Connect(function()
        for _, v in pairs(TabContainer:GetChildren()) do
            if v:IsA("ScrollingFrame") then v.Visible = false end
        end
        page.Visible = true
    end)
    return page
end

-- Criar Abas
local TabMain = CreateTab("Principal")
local TabXitos = CreateTab("XITOS DELTA")
local TabRecord = CreateTab("Gravações")
local TabCreditos = CreateTab("Criadores")

-- Abrir primeira aba automaticamente
TabMain.Visible = true

-- ==================== BOTÃO FLUTUANTE ====================
local FloatButton = Instance.new("TextButton")
FloatButton.Size = UDim2.new(0, 70, 0, 70)
FloatButton.Position = UDim2.new(0, 20, 0.5, -35)
FloatButton.BackgroundColor3 = Color3.fromRGB(0, 120, 255)
FloatButton.Text = "ST"
FloatButton.TextColor3 = Color3.new(1,1,1)
FloatButton.TextScaled = true
FloatButton.Font = Enum.Font.GothamBold
FloatButton.Parent = ScreenGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0.5, 0)
corner.Parent = FloatButton

FloatButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

-- ==================== FUNÇÕES ====================
local SaveFolder = "ST_Recordings"
if not isfolder(SaveFolder) then makefolder(SaveFolder) end

-- Função para criar botão fácil
local function AddButton(parent, text, color, callback)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(0.95, 0, 0, 55)
    btn.BackgroundColor3 = color or Color3.fromRGB(40, 40, 80)
    btn.Text = text
    btn.TextColor3 = Color3.new(1,1,1)
    btn.TextScaled = true
    btn.Font = Enum.Font.GothamSemibold
    btn.Parent = parent
    btn.MouseButton1Click:Connect(callback)
    return btn
end

-- ==================== ABA PRINCIPAL ====================
AddButton(TabMain, "🔧 Carregar F3X Building Tools", Color3.fromRGB(0,100,255), function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/CMD-X/F3X/main/F3X.lua"))()
end)

AddButton(TabMain, "🚶 Toggle NoClip", nil, function()
    -- Toggle NoClip aqui (simplificado)
    print("NoClip toggled (implemente se quiser)")
end)

AddButton(TabMain, "🟩 Textura Minecraft", nil, function()
    for _, obj in pairs(workspace:GetDescendants()) do
        if obj:IsA("BasePart") then
            obj.Material = Enum.Material.Grass
            obj.Color = Color3.fromRGB(100,180,50)
        end
    end
    print("Textura Minecraft aplicada")
end)

-- ==================== ABA XITOS DELTA ====================
AddButton(TabXitos, "🎯 ESP Tronco Rainbow", Color3.fromRGB(255,0,255), function()
    print("ESP Rainbow ativado (implemente)")
end)

-- ==================== ABA GRAVAÇÕES ====================
local RecordPage = TabRecord

AddButton(RecordPage, "▶ INICIAR GRAVAÇÃO", Color3.fromRGB(0,170,0), function()
    print("Gravação iniciada")
end)

AddButton(RecordPage, "⏹ PARAR GRAVAÇÃO", Color3.fromRGB(200,0,0), function()
    print("Gravação parada e salva")
end)

AddButton(RecordPage, "🔄 Atualizar Lista de Gravações", Color3.fromRGB(60,60,120), function()
    print("Lista atualizada")
end)

-- ==================== ABA CRIADORES ====================
local cred = Instance.new("TextLabel")
cred.Size = UDim2.new(0.95,0,0,400)
cred.BackgroundTransparency = 1
cred.Text = "Criadores da Hub:\n\nSTART9NZ\nVITINHO\n\nObrigado por usar o ST!"
cred.TextColor3 = Color3.new(1,1,1)
cred.TextScaled = true
cred.TextWrapped = true
cred.Parent = TabCreditos

-- Notificação Final
StarterGui:SetCore("SendNotification", {
    Title = "ST V4.7",
    Text = "Hub carregada com sucesso!\nBotão flutuante ativado",
    Duration = 8
})

print("ST V4.7 carregado - Todos os botões devem aparecer agora!")
